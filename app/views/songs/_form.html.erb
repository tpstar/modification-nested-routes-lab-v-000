<%= form_for @song  do |f| %>
  <%= artist_id_field(@song) %>
  <br>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <br />
  <%= artist_select("artist:", Artist.all) %>

  <%= f.submit %>
<% end %>
